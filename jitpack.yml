# https://jitpack.io/docs/BUILDING/

jdk:
  - openjdk9
before_install:
#  - ./custom_setup.sh
install:
  - echo "Running gradlew"
  - ./gradle/wrapper/gradlew clean install -xtest
env:
  key: "value"
